[project]
name = "place-clustering-agent"
version = "0.1.0"
description = "Place clustering agent for trip planning"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.8.0",
]

[tool.uv]
dev-dependencies = []