version: "3.9"

services:
  orchestrator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agent0_orchestrator

    ports:
      - "${AGENT_HOST:-0.0.0.0}:${AGENT_PORT:-8080}:8080"

    env_file:
      - .env

    volumes:
      - ${GOOGLE_CREDENTIALS_HOST_PATH:-./credentials}:/app/credentials:ro

    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/gcp-key.json

    restart: unless-stopped

    healthcheck:
      test:
        [
          "CMD", "python", "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8080/.well-known/agent.json')"
        ]
      interval: 30s
      timeout: 10s
      start_period: 15s
      retries: 3
