version: "3.9"
services:
  orchestrator:
    build: { context: ., dockerfile: Dockerfile }
    container_name: agent0_orchestrator
    ports: ["${AGENT_PORT:-8080}:8080"]
    env_file: [.env]
    restart: unless-stopped
