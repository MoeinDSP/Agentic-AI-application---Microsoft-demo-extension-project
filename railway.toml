[build]
builder = "dockerfile"
dockerfilePath = "place-recommender-agent/Dockerfile"
dockerContextDir = "place-recommender-agent"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
